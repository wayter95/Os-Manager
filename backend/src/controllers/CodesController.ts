import {Request, Response} from 'express';

import Knex from '../database/connection';

class CodesControllers {
    async create (request:Request, response:Response) {
        
    }
}

export default CodesControllers;